Build the project and sync with native Capacitor projects (iOS/Android).

Run: npm run cap:sync

This will:
1. Build the web project (npm run build)
2. Sync the dist/ output to native projects

Report success or failure. If it fails, analyze the error and suggest a fix.
