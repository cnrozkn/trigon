Run the project tests and report results.

If arguments are provided, run only the specified test file:
  node --test tests/$ARGUMENTS

If no arguments are provided, run all tests:
  node --test tests/

After running:
- Summarize pass/fail counts
- If any test fails, read the relevant source code and suggest a fix
