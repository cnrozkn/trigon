Give a concise project status overview.

Run these commands and summarize the results:
1. `git status` — show branch, staged/unstaged/untracked files
2. `git log --oneline -5` — last 5 commits
3. `node --test tests/` — run all tests and report pass/fail
4. Check if `dist/` exists and its size with `du -sh dist/` (if it exists)

Present everything as a short, scannable summary.
