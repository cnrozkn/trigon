Run a production build and report the result.

Steps:
1. Run `npm run build`
2. If the build fails, analyze the error output and suggest a fix
3. If the build succeeds, report the total size of the `dist/` directory using `du -sh dist/`
